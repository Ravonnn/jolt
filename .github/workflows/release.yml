name: Release

# Placeholder for Phase 15 (1.0 release artifacts).
on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Release workflow not configured yet."
